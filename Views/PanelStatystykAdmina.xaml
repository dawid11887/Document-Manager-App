<Window x:Class="DocumentManagerApp.Views.PanelStatystykAdmina"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DocumentManagerApp"
        Title="Panel Administratora" Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="None">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF24527A" Offset="0.0"/>
            <GradientStop Color="#FF5DACBD" Offset="1.0"/>
        </LinearGradientBrush>
    </Window.Background>
    <Border BorderBrush="Gray" BorderThickness="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <!-- Pasek -->
                <RowDefinition/>
                <!-- Zawartość -->
            </Grid.RowDefinitions>
            <!-- Pasek tytułowy -->
            <Grid Grid.Row="0" Background="#214866">
                <local:CustomTitleBarMini Grid.Row="0"/>
            </Grid>
            <Grid Grid.Row="1">
                <Grid>
                    <ContentControl x:Name="MainContent" />
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
